# Jenkins
Jenkins with docker

## Requirements
- docker
- docker-compose

```bash
docker-compose up

# stop
docker-compose down
```

## GUI
http://localhost:8080/  
Initial password is jenkins container outputs or /var/jenkins_home/secrets/initialAdminPassword
